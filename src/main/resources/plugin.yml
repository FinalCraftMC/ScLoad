name: ScLoad
main: me.fromgate.scload.ScLoad
version: 0.2.1a
depend: [WorldEdit]
author: [fromgate, Exemplive, EverNife]
commands:
   scload:
      aliases: [scl]
      description: ScLoad command
      usage: |
           /scload load <filename>
           /scload load <filename> <world>
           /scload reload
           /scload cfg
